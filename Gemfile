source "https://rubygems.org"

# Mandatory!! This gem replaces `jekyll` because we're using GitHub Pages.
# It comes with many plugins. See https://pages.github.com/versions/
gem "github-pages", "~> 232", group: :jekyll_plugins
